const config = {
  pgConfig: {
    user: 'root',
    database: 'postgres',
    password: 'root',
    host: 'localhost',
    port: 5432,
    max: 10,
    idleTimeoutMillis: 5000
  }
}

export default config
